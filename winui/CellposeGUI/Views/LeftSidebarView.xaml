<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="CellposeGUI.Views.LeftSidebarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    mc:Ignorable="d">
    <ScrollViewer HorizontalAlignment="Stretch" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="8" Spacing="8" HorizontalAlignment="Stretch">
            <Expander Header="Navigation" IsExpanded="True" x:Name="NavigationGroup" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <StackPanel x:Name="AxisPanel" Spacing="6" HorizontalAlignment="Stretch" />
            </Expander>

            <Expander Header="Views" IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <StackPanel Spacing="8" HorizontalAlignment="Stretch">
                    <ComboBox x:Name="ViewModeCombo" Header="View" HorizontalAlignment="Stretch" />
                    <Button x:Name="AutoSaturationButton" Content="auto saturation" HorizontalAlignment="Left" />
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="36" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="min" VerticalAlignment="Center" />
                        <Slider x:Name="GrayLowSlider" Grid.Column="1" Minimum="0" Maximum="255" StepFrequency="1" Value="0" HorizontalAlignment="Stretch" />
                        <TextBlock x:Name="GrayLowValueText" Grid.Column="2" Margin="6,0,0,0" VerticalAlignment="Center" Text="0" />
                    </Grid>
                    <Grid HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="36" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="max" VerticalAlignment="Center" />
                        <Slider x:Name="GrayHighSlider" Grid.Column="1" Minimum="0" Maximum="255" StepFrequency="1" Value="255" HorizontalAlignment="Stretch" />
                        <TextBlock x:Name="GrayHighValueText" Grid.Column="2" Margin="6,0,0,0" VerticalAlignment="Center" Text="255" />
                    </Grid>
                    <Grid HorizontalAlignment="Stretch" ToolTipService.ToolTip="0 = image only, 1 = mask only">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="36" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="40" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="blend" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Margin="4,0,4,0" VerticalAlignment="Center" Text="img" FontSize="10" Opacity="0.7" />
                        <Slider x:Name="MaskBlendSlider" Grid.Column="2" Minimum="0" Maximum="1" StepFrequency="0.01" Value="0.5" HorizontalAlignment="Stretch" />
                        <TextBlock Grid.Column="3" Margin="4,0,4,0" VerticalAlignment="Center" Text="mask" FontSize="10" Opacity="0.7" />
                        <TextBlock x:Name="MaskBlendValueText" Grid.Column="4" Margin="6,0,0,0" VerticalAlignment="Center" Text="0.50" />
                    </Grid>
                </StackPanel>
            </Expander>

            <Expander Header="Drawing" IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <StackPanel Spacing="8" HorizontalAlignment="Stretch">
                    <ToggleButton x:Name="BrushButton" Content="brush" HorizontalAlignment="Left" />
                    <ToggleButton x:Name="SelectButton" Content="select" HorizontalAlignment="Left" />
                    <Button x:Name="DeleteSelectedButton" Content="delete" HorizontalAlignment="Left" />
                    <Button x:Name="EditSelectedButton" Content="edit" HorizontalAlignment="Left" />
                    <NumberBox x:Name="DefaultClassBox" Header="default class" HorizontalAlignment="Stretch" SpinButtonPlacementMode="Inline" />
                </StackPanel>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</UserControl>
